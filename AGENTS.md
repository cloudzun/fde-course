# AGENTS.md — 本仓库的编写与迭代约定

> 本文档面向后续参与本仓库维护/迭代的协作者（含 AI 代理），说明**这套资料以什么方式、什么格式编写**，以及**后续更新迭代应采用什么方法**，以保证全书一致性与可维护性。

---

## 1. 仓库概况

- **内容**：《AI 时代的 FDE：认知、流程、团队与中国落地》——一套 FDE（前沿部署工程师）方法论与落地手册，六篇 16 章 + 附录。
- **当前版本**：v1.15（版本记录见 `misc/README.md`）。**唯一版本源以 `misc/README.md` 的修订记录为准**；`AGENTS.md`、`教材大纲目录`、`package.json` 等处的 "当前版本" 须与之保持一致。
- **正文**：`textbook/` 下 18 个 markdown 文件（00 封面导读 + 第01–16章 + 附录）。
- **合并版**：`misc/FDE通用培训教材_完整版.md`（单文件全文；**修订以 `textbook/` 分文件为工作底稿**，合并版仅在定稿/导出时更新）。
- **配套文档**：`misc/`（README 索引、大纲目录、交接小结、审校报告、修订指南、调研报告、阻塞项决议）。
- **许可**：CC BY-NC-ND 4.0——引用须署名、禁止商用、禁止修改后分发（仓库内迭代不属于对外分发，但对外发布任何形态的演绎/翻译均被禁止，需另行授权）。

---

## 2. 目录与文件规范

| 位置 | 内容 | 修改要求 |
| :--- | :--- | :--- |
| `textbook/` | 教材正文（唯一工作底稿） | 正文增删改只在这里做 |
| `misc/FDE通用培训教材_完整版.md` | 原始合并版 | 仅在需要"整体快照"时按章节拼接刷新；**不要作为工作底稿** |
| `misc/README.md` | 文件清单 + 版本记录 | 每次版本变更必须同步 |
| `misc/` 其他 | 大纲目录/交接小结/审校报告等 | 内容有实质变化时同步更新 |

**文件命名**：章节文件固定 `第NN章_<主题>.md`（两位数字序号）；新增章节用下一序号并同步 `misc/README.md` 文件清单与大纲目录。**文件名一旦确定不要随意改名**（改名会破坏链接与文档引用）。

---

## 3. 写作格式规范（markdown 约定）

### 3.1 标题层级（严格对应章节结构）
- `#` = 篇标题（仅出现在该篇首个章节文件开头，如 00 封面/第03章/第05章/第10章/第13章/第15章）
- `##` = 章标题（`## 第N章 ...`）
- `###` = 节（`### N.x ...`）
- `####` = 子节（`#### N.x.y ...`，仅当节内有独立子议题时使用）
- 禁止跳级（如 `##` 直接到 `####`）；每章结尾可有 `### 本篇收尾 · 甲乙方对照：<问题>`（不占编号）。

### 3.2 引用块（GitHub Alert 语法，按语义选用）
- `> [!NOTE]` 一般提示 / 定位说明
- `> [!TIP]` 操作建议
- `> [!IMPORTANT]` 关键结论 / 纪律
- `> [!CAUTION]` 风险提醒
- `> [!WARNING]` 反面模式

### 3.3 其他语法
- **表格**：必须带表头分隔行；长表格附"使用说明/读法指引"。
- **mermaid**：流程图/架构图用 ```mermaid 代码块。
- **公式**：`$...$` 行内、`$$...$$` 独立公式（如 2.3 经验曲线 Cₙ=C₁·n⁻ᵅ）。
- **检查清单**：`- [ ]` 复选框（Gate 检查清单、鉴别清单等）。
- **中文风格**：简体中文；正文用全角标点；术语首次出现给出英文原文。

### 3.4 术语统一表（新增内容必须沿用）
| 术语 | 全书统一口径 |
| :--- | :--- |
| PPT 框架 | 中文定名「人员流程技术」（People–Process–Technology），与演示文稿无关 |
| FDE | Forward Deployed Engineer，前沿部署工程师 |
| 能力回注 | 识别→抽象→集成→验证（四步法，第9章） |
| 四阶段 | Discovery→Prototype→Build→Scale（教材提炼；Palantir 官方体系在 How 总纲对照） |
| 角色 | 三角色（Delta/Echo/Engineering，前线编制）↔ 五角色（+Dev/Lead，教材扩展）；Echo/Delta 亦作第15/16章视角 |
| SOP | SOP 0–9，编号全局唯一 |

### 3.5 编号纪律（最容易出问题的环节）
- 新增小节用**顺延编号**（如现有到 N.7，新增 N.8），不占用已有编号。
- **一旦重编号**，必须用全文 grep `N\.[0-9]` 检索所有交叉引用并同步更新（含附录 G 研讨题、13.6/13.7 导航、14.5.2 三行业表、15/16 章互引等），并在 `misc/README.md` 修订记录中注明"重编号映射"。
- 交叉引用一律写"第N章 N.x"或"（N.x）"，不写模糊的"前文/后文"。

### 3.6 溯源纪律（防止"不可追溯"）
- **官方/事实性引文**：必须有可点击 URL 或附录对应条目（附录 A–D）。
- **教材提炼**：明确标注"本教材提炼/教材提炼"，不冒充任何厂商官方术语（如"五次部署法则""RICE 变体"均须标注）。
- **行业数字**：注明来源与时间点；无来源的量化断言要么给来源、要么弱化为定性并标注"经验表述/待核验"。
- **脱敏/口述内容**：标注"据受访者口述整理，已脱敏"或"综合提炼教学案例，非特指真实项目"。
- **时效性内容**（模型版本、市场数据等）：标注"信息截至 YYYY 年 MM 月"，落地前自行核对。

---

## 4. 版本与修订流程

### 4.1 版本号
- `v1.x` 递增（当前 v1.9）；重大重构/对外发布才升 v2.0。
- 每个版本阶段在 `misc/README.md` 的修订记录登记：**版本号 → 阶段名 → 改动摘要 → 联动更新项**。

### 4.2 修订模式（推荐：分批复核，避免一次大改）
1. **盘点问题**：先列问题清单（可基于审校报告），按严重度分 🔴（事实/引用/计数）→ 🟡（口径/信源）→ 🟢（锦上添花）。
2. **分批执行**：一批做完 → 全局核验（编号连续性 + 交叉引用 grep + 旧表述残留）→ 再开下一批。
3. **核验清单**：
   - 新增小节编号连续无重复；
   - 全文 grep `N\.[0-9]` 交叉引用全部有效；
   - 旧表述/旧编号正文零残留（历史文档中的记录可保留）；
   - 新增内容的引用目标小节确实存在；
   - 更新 `misc/README.md` 修订记录与（如需要）`教材大纲目录`。

### 4.3 内容增删改 SOP
- **新增章节/小节**：确定编号 → 按 3.1–3.6 写 → grep 交叉引用 → 更新 README 文件清单/大纲目录。
- **修改既有内容**：改 `textbook/` 分文件 → 同步检查相关引用 → 记录版本。
- **合并版刷新**（可选）：按"00 + 第01..16章 + 附录"顺序拼接，每文件首行降一级标题。

---

## 5. 迭代更新 SOP（给 AI 代理/协作者的执行清单）

每次迭代按以下顺序执行：

1. **读上下文**：先读 `README.md`（仓库说明）+ `misc/README.md`（版本与清单）+ 目标章节文件。
2. **定位改动点**：明确"改哪个文件、哪个小节、为什么"；涉及编号/引用的先做影响面分析。
3. **写**：遵守第 3 节全部格式与纪律；只改允许的文件（正文只动 `textbook/`；配套文档在 `misc/`）。
4. **核验**：按 4.2 核验清单跑 grep；修正发现的问题。
5. **记录**：更新 `misc/README.md` 修订记录（版本号 + 摘要 + 联动项）；涉及文件清单/大纲的变化同步更新。
6. **汇报**：列出改动文件、改动位置、核验结果。

---

## 6. 红线（必须遵守）

- **不直接改合并版**作为工作底稿。
- **不擅自重编号**而不更新全局交叉引用。
- **不新增不可溯源的"官方引文"**。
- **不对外分发**本仓库任何演绎/翻译形态（CC BY-NC-ND）；仓库内部协作与迭代不受影响。
- **不引入与既有主线矛盾的新概念**：新内容须与"卖人力→卖能力"主线、能力回注飞轮、四阶段 SOP、角色体系、甲乙方对照、中国语境保持一致。
